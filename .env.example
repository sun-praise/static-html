# Image tag to deploy. Override per host to pin a known-good version.
# - `latest` tracks the most recent build on `main` (rolling)
# - pin to a tag (e.g. `1.3.0`) for reproducible deploys and rollbacks
STH_IMAGE_TAG=latest
